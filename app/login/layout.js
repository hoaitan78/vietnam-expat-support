
export const metadata = {
    title: 'Login - Vietnam Expat Support',
    description: 'Sign in to your account to access community features.',
}

export default function LoginLayout({ children }) {
    return <>{children}</>
}
